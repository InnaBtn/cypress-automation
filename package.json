{
  "devDependencies": {
    "cypress": "^14.1.0",
    "mochawesome": "^7.1.3"
  },
  "scripts": {
    "test:stage": "cypress run --reporter mochawesome  --config-file cypress.config.stage.js",
    "test:prod": "cypress run --reporter mochawesome  --config-file cypress.config.prod.js"
  },
  "dependencies": {
    "cypress-real-events": "^1.14.0"
  }
}
